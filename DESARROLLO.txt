================================================================================
 LA BITAK0R4_ DE EZEQUIEL - LOG DE DESARROLLO
 Registro paso a paso de todo lo que se va construyendo.
================================================================================

[2026-09-07] FASE 1 - ESTRUCTURA COMPLETA
  - Scaffold con create-next-app@latest:
    * Next.js 16.3.4, React 19, TypeScript, Tailwind CSS v4, ESLint, App Router
    * El proyecto se creÃ³ en la subcarpeta "bitakra/" (npm no permite mayÃºsculas
      en el nombre del paquete) y se moviÃ³ todo a la raÃ­z E:\PROYECTO_BitAK0RA
  - Dependencias instaladas:
    * @supabase/ssr + @supabase/supabase-js -> conexiÃ³n con Supabase
    * @serwist/next + serwist -> PWA (service worker, offline)
    * recharts -> grÃ¡ficos de estadÃ­sticas
  - Git: repositorio inicializado por create-next-app en la raÃ­z.

  - Base de datos (Supabase):
    * supabase/schema.sql -> tablas: password_config, objectives, days,
      daily_objectives, temporal_goals. RLS habilitado sin polÃ­ticas: solo el
      service role key (backend) puede acceder.
    * supabase/seed.sql -> contraseÃ±a inicial "bitakra" + 3 objetivos de ejemplo.

  - ConfiguraciÃ³n:
    * next.config.ts -> envoltura conSerwist (PWA). swSrc app/sw.ts, swDest
      public/sw.js, cacheOnNavigation, register automÃ¡tico, deshabilitado en dev.
    * scripts: dev = "next dev --turbopack", build = "next build --webpack"
      (Serwist requiere webpack en build).
    * tsconfig: lib webworker + types "@serwist/next/typings" y "node".
    * .gitignore: public/sw*.js ignorados.
    * .env.local.example -> plantilla de variables de Supabase
      (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
       SUPABASE_SERVICE_ROLE_KEY).

  - PWA:
    * app/manifest.ts -> manifest.webmanifest (nombre, Ã­conos, standalone,
      colores).
    * app/sw.ts -> service worker Serwist.
    * scripts/gen-icons.mjs -> generÃ³ icon-192.png, icon-512.png y
      apple-touch-icon.png (placeholders de color sÃ³lido).
    * Se verificÃ³ que el <link rel=manifest> y theme-color se inyectan en el
      HTML.

  - LÃ³gica / capa de datos:
    * lib/supabase/server.ts -> cliente del lado servidor con service role key.
    * lib/supabase/client.ts -> cliente del navegador (anon key, para uso futuro).
    * lib/session.ts -> auth: hash SHA-256 (salt::password), cookie httpOnly
      "bitakra_session" de 30 dÃ­as, isAuthenticated() verifica contra Supabase.
    * lib/actions.ts -> Server Actions:
      - loginAction / logoutAction / changePasswordAction
      - saveDayNotes (upsert dÃ­a + notas)
      - toggleDailyObjectiveAction (check de objetivos por dÃ­a)
      - CRUD objectives (crear/editar/activar/borrar)
      - CRUD temporal_goals (crear/editar/activar/borrar)
    * lib/utils.ts -> helpers de fechas (ISO, calendario grid, labels en es).
    * lib/types.ts -> tipos de las tablas + ChecklistItem + DayStatsPoint.

  - Rutas / pÃ¡ginas:
    * proxy.ts -> protege /bitacora/* si no hay cookie de sesiÃ³n; redirige /
      a /bitacora si ya hay sesiÃ³n. (ConvenciÃ³n "proxy" de Next 16, reemplaza
      al deprecado middleware.)
    * app/page.tsx -> login (gate de contraseÃ±a) si no hay sesiÃ³n.
    * app/(bitacora)/layout.tsx -> header con nav (Hoy / Ajustes / Stats / Salir).
    * app/(bitacora)/page.tsx -> redirige a la fecha de hoy.
    * app/(bitacora)/[date]/page.tsx -> vista del dÃ­a: calendario + navegaciÃ³n,
      checklist de objetivos (con contador), metas temporales activas para esa
      fecha, notas con guardado.
    * app/(bitacora)/settings/page.tsx -> CRUD de objetivos, CRUD de metas
      temporales y cambio de contraseÃ±a.
    * app/(bitacora)/stats/page.tsx -> cumplimiento (%) de los Ãºltimos 30 dÃ­as
      (grÃ¡fico recharts), racha actual, dÃ­as con registro/notas.

  - Componentes:
    * password-gate, logoff-button, calendar-nav (dÃ­a prev/next + calendario
      mensual), objectives-checklist (useOptimistic), temporal-goals-section,
      notes-editor (estados idle/unsaved/saving/saved/error), objective-manager,
      temporal-goal-manager, change-password-form, stats-charts.

  - VerificaciÃ³n:
    * npm run lint -> sin errores ni warnings.
    * npm run build (webpack) -> compila OK, todo dinÃ¡mico, Serwist bundlÃ³ sw.js.
    * Nota: npm audit reporta 2 vuln high en browserslist (dependencia de build
      de @serwist/next). Es tooling de build, riesgo bajo. El fix requiere
      @serwist/next@9.4.1 (breaking change) -> se deja pendiente.

  - PENDIENTES (prÃ³ximos pasos para el usuario):
    1. Crear proyecto en Supabase.
    2. Copiar .env.local.example a .env.local y cargar las 3 claves.
    3. Correr supabase/schema.sql y supabase/seed.sql en el SQL Editor.
    4. npm run dev y entrar con la contraseÃ±a inicial "bitakra"
       (cambiarla despuÃ©s desde Ajustes > Seguridad).
    5. Deploy en Vercel (link del repo, variables de entorno, build --webpack
       ya configurado).
    6. EstÃ©tica / tema visual (pensado como fase final).

[2026-09-08] FASE 2 - MODO LOCAL + RUTAS + NOTAS POR DÃA
  - BUG IMPORTANTE corregido: el grupo de rutas "(bitacora)" NO agrega segmento
    a la URL (nomenclatura de rutas de Next), asÃ­ que /bitacora daba 404 y
    /bitacora/[date] no existÃ­a. Se renombrÃ³ app/(bitacora)/ -> app/bitacora/.
    Ahora las rutas son /bitacora, /bitacora/[date], /bitacora/settings y
    /bitacora/stats (se corrigieron path del log FASE 1 arriba: es bitacora/).
  - Modo local sin Supabase: lib/local-client.ts emula el cliente (select/eq/
    in/gte/lte/order/single/maybeSingle/insert/upsert/update/delete), persiste
    en .data/local-db.json (gitignored). Se activa solo si falta la URL de
    Supabase o con LOCAL_MODE=1. Password inicial local: "bitakra".
    * lib/supabase/server.ts -> createClient() elige local o Supabase; expone
      isLocalMode().
    * app/bitacora/layout.tsx -> badge "LOCAL Â· SIN DB" cuando corresponde.
    * Login muestra hint de contraseÃ±a local en modo local (PasswordGate).
  - Notas: se reemplaza el campo Ãºnico days.notes por una TABLA notes (id, date,
    content, created_at). Ahora cada dÃ­a acumula varias notas.
    * supabase/schema.sql -> nueva tabla notes (+ Ã­ndice date,created_at) y se
      dropea days.notes.
    * lib/actions.ts -> addNoteAction(date, content) y deleteNoteAction(id, date)
      reemplazan a saveDayNotes.
    * components/notes-editor.tsx -> lista cronolÃ³gica de notas ENCIMA del
      textarea, cada una con hora y botÃ³n Eliminar; se agregan con useOptimistic
      (append), mensaje "Nota agregada" y limpieza del textarea.
    * El id de cada nota lo genera el CLIENTE (crypto.randomUUID) y se manda en
      el insert: asÃ­ el id optimista == id del server y borrar siempre acierta
      aunque el refresh del server tarde (evita carreras del useOptimistic).
      addNoteAction(date, content, id).
    * app/bitacora/[date]/page.tsx -> consulta notas del dÃ­a, NotesEditor con
      key={date} para resetear al navegar entre dÃ­as.
    * stats: "DÃ­as con notas" y "hasData" ahora se calculan desde la tabla notes.
  - Fix warning de React: "An optimistic state update occurred outside a
    transition" en objectives-checklist: toggleOptimistic() ahora va DENTRO del
    startTransition junto a la acciÃ³n.
  - ESLint: se ignoran scripts/** (herramientas de testeo: scripts/smoke-test.js).
  - Frase motivacional: components/motivational-quote.tsx + lib/quotes.ts (12 frases
    cortas en espaÃ±ol con autor). Recuadro justo debajo de la fecha en
    app/bitacora/[date]/page.tsx: cita con comillas Â«Â» en itÃ¡lica, autor normal.
    Rota por dÃ­a de forma determinÃ­stica (hash de la fecha -> Ã­ndice): cada dÃ­a
    muestra una distinta, estable durante todo el dÃ­a.
  - Verificado: npm run build (webpack) compila con las rutas correctas; lint OK.

[2026-09-08] FASE 3 - MÃS FRASES + MARCADO DE DÃAS CUMPLIDOS
  - Frase motivacional v2: pool expandido de 12 a ~96 frases en lib/quotes.ts
    (pool local en vez de API externa: la PWA debe andar offline y sin rate
    limits). Nuevo getRandomQuote() y botÃ³n "â†» Otra frase" en
    components/motivational-quote.tsx (ahora client component, useState), que
    refresca la cita al instante. La del dÃ­a sigue siendo determinÃ­stica por
    fecha (quoteForDate).
  - Marcado de dÃ­as cumplidos en el calendario:
    * Tabla nueva settings (fila Ãºnica id=1): completion_mode ('off'|'count'|
      'percent') + threshold. En supabase/schema.sql (con insert default) y
      seed en lib/local-client.ts. FIX: load() ahora se asegura de que la fila
      singleton de settings exista aunque la DB local se haya creado antes.
    * lib/completion.ts -> isDayFulfilled(config, total, completed): modo
      'count' (objetivos >= threshold) o 'percent' (% >= threshold).
    * lib/actions.ts -> saveSettingsAction revalida settings + bitacora.
    * components/completion-settings-form.tsx -> en Ajustes: criterio
      (desactivado / por cantidad / por %) + valor mÃ­nimo.
    * components/calendar-nav.tsx -> prop marks (Record<fecha, bool>):
      dÃ­a cumplido = punto verde esmeralda â— bajo el nÃºmero (+ title "DÃ­a
      cumplido"). Las celdas mantienen el * del dÃ­a actual.
    * app/bitacora/[date]/page.tsx -> calcula marks: settings + total de
      objetivos activos + daily_objectives de todos los dÃ­as (solo si modo
      distinto de off). Nota: el "total" de un dÃ­a se estima con los objetivos
      activos actuales.
  - Verificado en dev: tsc, lint, build (webpack) y smoke-test completo
    (refresh de frase, guardado de ajustes, recuadro verde, notas, stats).
    PENDIENTE AL CONECTAR SUPABASE: sigue el item de updated_at en inserts.

[2026-09-08] FASE 7 - OJO 3D IMPONENTE + APAGADO CRT AL ERRAR EL LOGIN
  - Ojo reescrito como render 3D real (components/login/ascii-eye.tsx):
    por celda se proyecta al elipsoide del globo ocular y se calcula la
    normal -> luz difusa desde arriba-izq + brillo especular + atenuacion de
    limbo, y la luminosidad se mapea a una rampa de densidad ASCII
    (Â· . : - = + * # % @). Mucho mas grande: grid 52x30 celdas, canvas
    832x510 CSS (min(92vw, 832px)).
  - Iris/pupila como disco SOBRE la superficie de la esfera orientado por el
    vector de mirada (G): al mirar de costado el patch se recorre hacia el
    borde del globo (profundidad real). Iris en anillos concentricos
    (limbo oscuro, medio, interno), pupila con glint humedo arriba-izq.
  - Parpados como ocultadores 3D: parpado superior con ceja marcada y arruga
    (arco que sigue el globo, mas alto hacia afuera) y linea de pestanas
    brillante que se adelgaza al cerrar; el cerrado colapsa en una fina linea
    de luz central. Parepado inferior leve.
  - El ojo se ve ENTERO: la piel del parpado superior (cuenca) se extiende por
    fuera del globo hasta la arruga, la del inferior envuelve la base, y los
    CANTOS (puntas) quedan definidos por piel periorbitaria que rodea cada
    extremo del globo (sin marco/anel posterior, solo ceja marcada).
  - Miradas: al inicio lentas/solemnes (3-5s); ROUND 2 -> mas rapidas y
    MARCADAS hacia los costados: sacadas cada ~1.2-2.2s con targets laterales
    fuertes (hasta +-0.85 en X, +-0.6 en Y) y snaps mas veloces (k dt*7).
    El parpadeo sigue siendo espaciado (~6-10s).
  - ROUND 3 (mirada fija alternada): se elimina la sacada aleatoria. El ojo
    mira SIEMPRE a un lado fijo (+-0.8 horizontal) y CADA parpadeo cambia al
    lado opuesto: el flip ocurre con el parpado totalmente cerrado (en el
    hold del blink), asi "parpadea y mira al otro". Parpadeos mas rapidos
    (cada ~1.5-2.8s, duracion ~340ms) para que el ritmo sea vivo.
  - ROUND 4 (forma + mirada): se eliminan las "alas" laterales (cantos y
    cuenca anchos); el tejido periorbitario queda finito y pegado al globo
    (cuenca delgada arriba/abajo + arruga sutil + punta chica en cada canto).
    Se quita el brillo especular de la esclera (solo queda el glint humedo
    sobre la pupila). La mirada ya no es solo izquierda/derecha: cicla un
    set de 8 direcciones fijas (costados, arriba 3/4, abajo 3/4) y cada
    parpadeo avanza a la siguiente direccion (con el ojo cerrado). Solo al
    errar el login mira directo al frente y sale el fade out.
  - scripts/render-eye.js (dev-tool, exento de ESLint por scripts/): replica
    la misma matematica sin canvas e imprime el ojo como ASCII en la terminal
    (estados center/right/left/up/down/half/blink/stare) para iterar el arte
    sin ver capturas. Con la rampa Â· . : - = + * # % @  y los %/@/#/tilde
    _ del parpado/ceja.
  - Titulo corregido: "Â¿Sos el que tiene que ser?" (antes "quien").
  - Login con maquina de estados (components/login/login-screen.tsx, nuevo):
    idle -> wrong -> flash -> off. PasswordGate agrega onError (detecta
    error de loginAction una vez por intento) y disabled para no reenviar.
  - Al errar la contraseÃ±a: el ojo fija la mirada AL FRENTE (~320ms, prop
    stare del AsciiEye -> target 0,0, micro-vida minima y sin sacadas) y
    luego se reproduce el apagado de televisor viejo (components/login/
    crt-shutdown.tsx): destello blanco corto con scanlines oscuras -> la
    imagen brilla y COLAPSA hacia el centro con curvatura (reloj de arena) y
    parpadeo de fosforo -> linea central que se acorta parpadeando -> NEGRO
    ABSOLUTO. El apagado NO es un rectangulo blanco plano: la banda se curva,
    parpadea y se aplasta tipo TV. Sin texto ni boton; hay que refrescar la
    pagina para reintentar.
  - El contenido (lluvia, form) se desmonta al llegar a "off"; queda solo un
    div negro fijo. app/page.tsx ahora monta LoginScreen.
  - Smoke test ampliado: clave incorrecta -> aparece el overlay negro, el
    input desaparece, y tras refrescar el login vuelve. Todo verde + sin
    errores JS (tsc, lint, build webpack).

[2026-09-08] FASE 6 - LOGIN VISUAL (solo /)
  - Fondo negro + lluvia de caracteres monocroma (blanco cabeza -> grises ->
    casi transparente): canvas fixed full-viewport (components/login/
    rain-backdrop.tsx), RAF ~28fps, charset alfanum + katakana + simbolos,
    pausa con visibilitychange, respeta prefers-reduced-motion.
  - Ojo ASCII animado al centro (components/login/ascii-eye.tsx): forma
    almendrada procedimental (elipse con esquinas), esclerotica sombreada con
    luz desde arriba-izq, iris + pupila brillante que se mueve por sacadas
    aleatorias (cambia de direccion cada 0.9-2.6s) y parpadea cada ~4-7s
    (parpados superior/inferior + linea de ojo cerrado). Halo oscuro radial
    detras para destacarlo de la lluvia.
  - Titulo pixel "Â¿Sos quien tiene que ser?" con fuente Press Start 2P
    (next/font/google self-hosted, fallback monospace) -> util font-pixel.
  - Input password oscuro de una linea + boton flecha -> al final (type submit,
    aria-label "Entrar"); sigue name=password/required/autoFocus.
  - Los selectores del smoke test no cambian (login sigue pasando).
  - Ajuste visual: lluvia mas densa (GAP 11, FONT 13) y mas rapida
    (2.1-5.3 px/frame), mas al fondo con opacity-70 + blur 2px. Ojo mas 3D:
    iris en anillos concentricos (@ o 0 : hacia la pupila), pupila con
    brillo (glint) arriba-izq, sombra de parpado superior, rebote de luz
    inferior, contorno tenue de la cuenca y sclera con luz direccional.

[2026-09-08] FASE 5 - CUADRADO PARTICIONADO (3 COLORES) + SECCIÃ“N "QUÃ‰ APRENDÃ"
  - Cuadrado del dÃ­a: sÃ³lido y mÃ¡s grande (h-7/min-w-7), se particiona en
    franjas de igual tamaÃ±o segÃºn lo que se hizo ese dÃ­a:
      * verde  = dÃ­a cumplido (objetivos, segÃºn Ajustes)
      * amarillo = hay notas
      * rojo   = hay aprendizajes ("quÃ© aprendÃ­")
    1 sola cosa -> 100% un color; 2 -> 50/50; 3 -> 33/33/33. El nÃºmero del
    dÃ­a se superpone con texto blanco + sombra.
  - Nueva tabla learnings (id, date, content, created_at) como notes.
    Actions addLearningAction/deleteLearningAction (mismo patrÃ³n, id generado
    en el cliente con crypto.randomUUID).
  - Editor compartido DailyEntriesEditor: notas y aprendizajes reutilizan el
    mismo componente (lista cronolÃ³gica + textarea + botÃ³n + useOptimistic).
    NotesEditor y LearningsEditor ahora son wrappers.
  - PÃ¡gina de dÃ­a: los 2 editores van lado a lado (grid md:grid-cols-2).
    Fix React: keys Ãºnicas `notes-${date}` / `learnings-${date}` (eran iguales
    y React tiraba "two children with the same key").
  - Marcado del calendario: 3 sets (completados por config de Ajustes, con
    nota, con aprendizaje) por mes visible -> marks[date] = DayMark.
  - Stats: nueva tarjeta "DÃ­as con aprendizajes" (hasData incluye learnings).

[2026-09-08] FASE 4 - FRASES REALES CON ROL + MARCADO NOTORIO
  - Frases v3: pool re-curado con SOLO frases reales y/o refranes populares
    (~60), eliminando las inventadas. Cada cita suma el rol del autor: ej.
    "â€” SÃ¸ren Kierkegaard, filÃ³sofo danÃ©s". Los "anÃ³nimos" ahora son refranes
    populares verificables ("RefrÃ¡n popular"/"Proverbio chino") o frases
    famosas anÃ³nimas. El tipo MotivationalQuote pasa a author + role? y el
    footer las muestra separadas por coma.
  - Marcado de dÃ­as cumplidos: en vez del puntito, el dÃ­a cumplido se resalta
    con un CUADRADO verde cerrado alrededor del nÃºmero (span inline con
    border-2 border-emerald-500 y h-6/min-w-6, mÃ¡s chico que la celda),
    visible junto al dÃ­a seleccionado y al * del dÃ­a actual.
  - PENDIENTE AL CONECTAR SUPABASE: conDefaults() de local-client agrega
    updated_at a inserts; las tablas objectives y temporal_goals no tienen esa
    columna -> revisar si al insertar en Supabase real da error (local no afecta).
[2026-09-08] FASE 8 - MAQUETAS VISUALES (public/maquetas.html)
  - Pagina estatica autocontenida servida en /maquetas.html, sin login.
    Muestra la vista dia completa (calendario, cita, objetivos, metas,
    notas, aprendizajes) en 5 temas que rotan automaticamente cada 8s
    con controles (prev/next/dots/play-pausa/flechas de teclado).
  - ROUND 1: 4 temas (CRT terminal, Neon, Papel, Pixel 8-bit). El usuario
    los encontro muy parecidos entre si; le gusto solo el de papel y pido
    variedad usando su mundo de referencia (oleo, renacimiento, escultura,
    sobrio, verde ftalo).
  - ROUND 2 (definitivo), 5 temas:
    1. Papel (cuaderno de tinta claro, serif Georgia, guias pautadas).
    2. Carbonilla (reversion del papel: cuaderno oscuro, tinta sepia,
       sello en negativo).
    3. Al oleo (lienzo texturizado con marco de galeria, doble moldura con
       filo dorado, pinceladas/pigmento verde ftalo + oxido + ocre en las
       esquinas, calendario con marcas redondas tipo mancha).
    4. Renacimiento (marmol con vetas, verde renacimiento + oro, fuente
       Cinzel, cabecera centrada con floron, hoy como medallon dorado).
    5. Sobrio verde ftalo (fondo ftalo profundo, un unico acento ftalo
       claro, headers uppercase con tracking, cards solo con linea fina).
- Verificado con playwright-core: 5 temas rotan, dots/prev/next OK,
     sin errores JS; tsc y lint verdes (no se toco codigo TS).
   - ROUND 3 (6 temas): el usuario pidio un rediseno ESTRUCTURAL agresivo
     (no mas variaciones solo de color) + estetica cyberpunk/retro en blanco
     y negro con neon. Se conserva el esquema del ROUND 2 pero ahora cada
     tema es una composicion distinta:
     1. Papel (cuaderno claro, tinta azul noche, serif Georgia, guias).
     2. Carbonilla (reversion del papel: cuaderno oscuro, tinta sepia,
        sello en negativo roto).
     3. Al oleo - GALERIA (pagina completa de museo: pared con franja
        verde ftalo del piso, cuadros ENMARCADOS colgados en composicion
        asimetrica con molduras dobles y filo dorado, placa de museo debajo
        de cada cuadro, copete "SALA VERTICAL No. 3").
     4. Renacimiento - FRONTISPICIO (portada de libro: fronton + floron
        dorado, marmol con vetas, cabecera centrada en mayusculas con
        numeros de "voto" en romanos I/II/III, hoy como medallon dorado,
        folios laterales con filigrana).
     5. Verde ftalo - EDITORIAL (revista sobria: masthead, numero de dia
        GIGANTE "08", reglas finas ftalo, mucho aire, headers uppercase).
     6. Cyberpunk HUD 2026 (fondo negro, solo blanco + verde acido #00ff9d,
        fuente Geist Mono, scanlines por CSS, reloj display y LED, glitch
        animado en la cita, ticker marquee de "SISTEMA EN LINEA", bloques
        con esquinas en L, cursor bloque parpadeante, cellular 240x320).
   - Verificado con playwright-core: 6 dots, 6 secciones en orden, next
     cicla 0->5 y prev vuelve a envolver al 6, un solo dot activo, indicator
     "N Â· Nombre Â· N/6", play/pausa alterna, auto-rotacion post-reload (pos
     0 -> 1 a los 8.5s), y por tema: fondo/fuente distintos y elementos
     estructurales presentes (6 molduras+6 placas en galeria, folios con
     filigrana en renacimiento, masthead editorial, glitch+ticker+mono en
cyberpunk). Sin errores JS. tsc y lint verdes (HTML/CSS/JS estatico,
      no se toco codigo TS).
   - ROUND 4 (Cyberpunk x10 colores): el usuario elige el tema Cyberpunk
     como su favorito y pide 10 variaciones de color del neon (la primera
     que quiere ver: BLANCO). Decision de UX: cada variacion es un slide
     propio en la rotacion (15 temas totales, 15 dots).
   - CSS: todo el bloque .t-cyb (~90 reglas) refactorizado de hexes
     hardcodeados a CSS custom properties definidas en .t-cyb:
     --cyb-bg/#030303, --cyb-fg/#f2f2f2, --cyb-neon (literal del accent),
     --cyb-neon-rgb (tripleta para rgba(var(--cyb-neon-rgb),.45)), grises
     (--cyb-mut/dim/faint/deep), bordes/paneles (--cyb-lines/deck-bg/
     panel/blk/btn/num/track-bg/track/field/border) y glitch
     (--cyb-g1/#ff3b5c, --cyb-g2/#00e5ff). Se mantienen fijos: scanlines,
     vinheta, LED amber/rojo semanticos, seg a/r del calendario.
     10 variantes solo cambian las 2 variables del neon:
     cyb-verde #00ff9d, cyb-blanco #f2f2f2, cyb-cian #00e0ff,
     cyb-ambar #ffb020, cyb-rojo #ff3b5c, cyb-fucsia #ff4fd8,
     cyb-violeta #b26bff, cyb-azul #4d8fff, cyb-amarillo #ffd21e,
     cyb-naranja #ff7a2e.
   - HTML: el section 6 quedÃ³ como `mock t-cyb cyb-verde` y se duplico
     9 veces (via script node en temp, UTF-8) con data-name "6..15 Â·
     Cyberpunk Â· <color>". El rotador es data-driven: 15 dots, indicator
     "N Â· Nombre Â· N/15" sin tocar el JS.
   - Verificado con playwright-core: 15 dots / 15 secciones en orden, next
     cicla 0->14, prev envuelve al 15, un solo dot activo, play/pausa,
     auto-rotacion post-reload. Por variante: --cyb-neon computado coincide
     y la cascada recoloriza ticker (bg invertido), celda del dia, borde de
     checkbox, caret y barras de progreso; glitch y scanlines presentes en
     las 10. Sin errores JS. tsc y lint verdes.
   - ROUND 4.1 ajustes pedidos por el usuario sobre la variante blanco:
     * En el calendario lo blanco de los dias vuelve al verde acido:
       .t-cyb.cyb-blanco .cell .seg i.g y .cell.today .numwrap -> #00ff9d
       (sombra rgba(0,255,157,.45)). Segs ambar/rojo siguen igual.
     * El LED "SYS.OK" (i.led base) en blanco vuelve al verde acido via
       .t-cyb.cyb-blanco .led:not(.amber):not(.red) (los LEDs amarillo y
       rojo de LOCAL/NO-DB quedan semanticos).
     * Se elimina el cursor bloque parpadeante (span.caret) que aparecia
       junto al boton "Agregar nota" en los 10 tema cyberpunk y su regla
       CSS .t-cyb .caret (dead code). El boton queda solo.
   - Verificado con playwright-core: en "7 Â· blanco neon" la celda del dia,
     seg g y SYS.OK computan rgb(0,255,157), ticker sigue blanco, LEDs
     amber/red sin cambios, .caret count 0 en toda la pagina, send a la
     vista, ninguna regla .caret en los stylesheets. Sin errores JS.
     tsc y lint verdes.
   - ROUND 5 (restyling COMPLETO de la app real al tema elegido): el
     usuario confirma la variante BLANCO NEON para toda la bitacora.
     Decisiones fijadas: alcance = TODA la app (header HUD + vista dia +
     Ajustes + Stats; login queda con su CRT intacto) y las frases
     cripticas rotan en cada carga Y al errar la clave.
   - app/globals.css: base cyberpunk. :root --background/#030303 y
     --foreground/#f2f2f2, body oscuro + Geist Mono. Wrapper .cyb-shell
     define las custom properties (--cyb-neon/f2f2f2, --cyb-green/#00ff9d,
     --cyb-amber/#ffe14a, grises mut/dim/faint/deep, bordes lines/deck-bg/
     panel/blk/btn/num/track-bg/track/field/border, glitch g1/g2) +
     scanlines y vinheta fijas (::before/::after, z 5/6). Helpers: .blk
     (esquinas en L), .blk-tag (// TITULO), .cyb-btn/.cyb-btn.small/.tight,
     .cyb-in (inputs oscuros caret neon), .cyb-check (cuadrado con âœ•),
     .cyb-link/.cyb-link.red, .led/.led.amber/.led.red, .cyb-ticker
     (marquee blanco invertido), .glitch (RGB split con data-text),
     .enrow, .cal-btn, .cyb-num + segs-g/a/r, regla .today verde acido.
   - app/bitacora/layout.tsx: header HUD sticky (.cyb-deck) con logo
     "LA_BITAK0R4_", reloj HH:MM:SS con segundos parpadeantes, LEDs
     SYS.OK (verde acido) / LOCAL-SUPA (ambar) / NO-DB-SYNC (rojo, segun
     isLocalMode) y nav neon (Hoy/Ajustes/Stats/Salir). Ticker
     "SISTEMA EN LINEA" bajo el header y stage centrado (max-w 860px).
   - components/cyb-clock.tsx: nuevo, reloj real 1s, hydrate-safe (render
     "--:--:--" hasta el mount para evitar hydration mismatch).
   - Vista dia app/bitacora/[date]/page.tsx: cards .blk con tags
     "Agenda Â· <mes>", "Votos Â· x/y", "Notas_del_dÃ­a" y "Aprendizajes";
     TemporalGoalsSection es .blk "Metas_activas".
   - calendar-nav.tsx: celdas HUD sin fondo de bloque; el numero vive en
     .cyb-num con segs verticales g/a/r y la celda de hoy en verde acido
     con numero blanco (igual al mockup: .has-segs pisa el color oscuro).
   - motivational-quote.tsx: blockquote .blk con cita .glitch (data-text),
     autor cyb-muted y boton "â†» Otra frase".
   - objectives-checklist.tsx: checkbox .cyb-check cuadrado neÃ³n, titulo
     tachado + cyb-muted al completarlo, empty state cyb-hint.
   - daily-entries-editor.tsx (notas/aprendizajes): filas .enrow, hora
     cyb-hint, Eliminar .cyb-link red, textarea .cyb-in, boton .cyb-btn
     (sin caret), mensajes #00ff9d / #ff3b5c.
   - Ajustes: completion-settings-form, objective-manager, temporal-goal-
     manager y change-password-form en .blk con tags "//" y cyb-in/cyb-btn/
     cyb-link; logoff-button vinculo rojo en el nav. Stats: cards .blk y
     stats-charts con barras #00ff9d, grilla #2a2a2a, ticks #8f8f8f y
     tooltip oscuro.
   - Login: nuevo lib/login-phrases.ts (pool criptico: "Â¿Sos el que tiene
     que ser?", "Â¿Sos aquel que debe ser?", "Â¿QuiÃ©n sos?", "Â¿Sentiste tu
     llamado?", "Â¿Viste la seÃ±al?", "No todo es lo que parece. Â¿EntrÃ¡s?",
     etc, sin repetir la inmediata anterior) rotando en mount (useEffect,
     para no romper hydration) y en handleError al errar la clave.
     password-gate: input HUD con border/focus/caret #00ff9d.
- Verificado: tsc, lint y next build --webpack verdes. Smoke test
      actualizado a los nuevos selectores (.cyb-deck, .blk, i.segs-g/a/r,
      "Votos", etc) PASANDO TODO: login frases + CRT off en clave erronea,
      HUD/reloj/LED/ticker, cita glitch rotando, checkbox HUD, settings
      count>=1, notas y aprendizajes con segs y persistencia, stats,
      sin errores JS (el hydration mismatch del login y del reloj se
      resolvieron con montaje post-hydration). Probe de estilos computados:
      body #030303 mono, celda hoy rgb(0,255,157) con glow y numero blanco,
      seg g idem, ticker #f2f2f2/#030303 invertido, SYS.OK verde acido,
      .caret count 0.
[2026-09-08] FASE 9 - DINO CHAT (acceso desde la bitacora)
    - lib/dinosaurs.ts: registro de 6 dinos (tyra, nod0, 4nky, v3l0, br4ch0,
      tr1l0) con head ASCII 9x9 generado de forma uniforme y verificada por
      checker automatico (mismo alto, caracteres ASCII).
    - lib/dinos/*.md (x6): personalidad de cada dino con REGLA DE VOZ en
      "Frase de registro" (frase rioplatense real aprobada dia a dia).
    - lib/dino-prompt.ts: arma el system prompt leyendo el .md (cache en
      memoria); reglas duras de registro rioplatense WhatsApp: voseo, frases
      cortas (2-4 oraciones), sin saludos/despedidas formales, sin emojis,
      sin muletillas de IA.
    - app/api/chat/route.ts: streaming a OpenRouter con autenticacion,
      historial sanitizado (max 20 msgs), sanitizado de campo, retorno de
      error en stream. Modelos: env (google/gemma-4-31b-it:free) con cadena
      de fallback (gemma-4-26b-a4b-it:free, dots-studio/dots-3-note-preview:
      free, thinkingmachines/inkling-small:free), backoff 900ms y mensaje
      "Todos los modelos gratis estan saturados" (429) si ninguno anda.
      temperature 0.5, max_tokens 800, ATTEMPT_TIMEOUT_MS 25s.
    - components/dino-chat/dino-chat.tsx: dock flotante (solo la cara ASCII,
      sin label) abajo a la derecha; panel de chat con lista de dinos,
      cambio de contacto, sesion en sessionStorage (db_dino_chat_v1,
      DEFAULT_DINO "nod0", MAX_THREAD 40). El dock desaparece cuando el
      panel esta abierto; solo se cierra con la X (o ESC).
    - app/globals.css: clases .cyb-dock, .cyb-dchat, .cyb-msg*, etc (blanco
      neon). Integrado en app/bitacora/layout.tsx.
    - .env.local / .env.local.example: OPENROUTER_API_KEY (solo .local),
      OPENROUTER_MODEL y SITE_URL. next.config.ts: outputFileTracingIncludes
      para que los .md de lib/dinos entren al trace de build de /api/chat.
    - Verificado: tsc, lint, build, probe del endpoint (200 con respuesta
      rioplatense real) y smoke con el panel de chat completo.
[2026-09-08] FASE 10 - DÃAS FUTUROS SOLO LECTURA + RECORDATORIOS
    - DÃ­as futuros bloqueados: en app/bitacora/[date]/page.tsx, si date > hoy
      se muestra banner "Vista futura Â· solo lectura" y se ocultan Votos,
      Notas y Aprendizajes (dato gris). Las celdas futuras del calendario
      salen grises (.cyb-num.future) sin hover. Guarda de servidor en
      lib/actions.ts: addNoteAction, addLearningAction y
      toggleDailyObjectiveAction rechazan fecha > hoy ("DÃ­as futuros: solo
      lectura.").
    - Entidad reminders: tabla en supabase/schema.sql (id, date, content,
      created_at, indice por date, RLS), tabla "reminders" en
      lib/local-client.ts (TABLES + defaultSeed vacio), tipo Reminder en
      lib/types.ts.
    - lib/actions.ts: createReminderAction(date, content, id),
      updateReminderAction(id, content, date) y deleteReminderAction(id, date)
      con requireAuth + revalidatePath. Sin bloqueo de futuro (el recordatorio
      es un plan).
    - components/reminder-panel.tsx (client): lista con Editar (inline) /
      Borrar, caja "nuevo recordatorio" con campo fecha dd/mm/aa (parse
      parseShortDate en lib/utils.ts, formato formatShortDate), validacion y
      guardado optimista. Se reusa en el modal del calendario y en la tarjeta
      del dÃ­a.
    - components/calendar-nav.tsx: long-press (550ms) sobre cualquier celda
      abre el modal con la fecha prellenada (se cancela si soltas o movÃ©s;
      user-select none + sin contextmenu nativo). BotÃ³n REC+ en la fila del
      aÃ±o abre el modal con fecha propia textual. Recuadro rojo
      (.cyb-num.has-reminder) en dÃ­as con recordatorio activo (date <= hoy);
      los futuros se ven/editan/borran desde el modal pero no marcan el
      calendario; los vencidos quedan en la tarjeta del dÃ­a sin recuadro.
    - Tarjeta "Recordatorios" en la pÃ¡gina del dÃ­a (acento rojo .reminder-blk
      si hay activos) con el mismo panel editable.
    - app/globals.css: .cyb-num.future, .has-reminder, .cyb-cal-grid
      (user-select) y .cyb-modal / .cyb-modal-panel (overlay fijo z100, encima
      incluso del chat), .blk.reminder-blk, .blk.future-blk.
    - Verificado: tsc, lint, next build --webpack. Smoke extendido PASANDO
      TODO: vista futura sin editores y celda gris, long-press abre modal,
      crear/editar/borrar recordatorio, recuadro rojo aparece y persiste tras
      reload, REC+ con fecha propia dd/mm/aa, borrado desde el dÃ­a futuro.
      Sin errores JS.

FASE 11 (2026-09-08):
  - Votos diarios con 3 estados: daily_objectives pasa de boolean completed a
    texto status ('none' | 'partial' | 'done') con check constraint en
    supabase/schema.sql. lib/types.ts: ChecklistStatus, DailyObjective.status,
    ChecklistItem.status. lib/completion.ts: statusOf() migra filas viejas
    (completed:true -> 'done').
  - lib/actions.ts: toggleDailyObjectiveAction -> setDailyObjectiveStatusAction
    ({date, objectiveId, status}) con upsert y guard de dia futuro.
  - components/objectives-checklist.tsx: 3 slots clicables por objetivo
    (rojo X = sin hacer, ambar '-' = a medias, verde check = hecho), rol radio,
    optimista. Ya no hay checkbox.
  - Pagina del dia: completedCount cuenta status 'done'; stats (percent) usa
    statusOf + done. Select daily_objectives pasa a '*' para compat.
  - Recordatorios: la tarjeta del dia ya no muestra el campo de fecha (esta
    fijado al dia). Se quita el boton REC+ del calendario; nueva
    components/reminder-fab.tsx: rectangulo fijo abajo a la izquierda (REC+
    / 'fecha propia') que abre el modal con fecha libre, integrado en
    app/bitacora/layout.tsx. CSS: .cyb-trio, .cyb-trio-slot.tri-none/partial/done,
    .cyb-rem-fab.
  - Smoke actualizado (slots trios + FAB) PASANDO TODO, tsc + lint + build OK.
    Sin errores JS.

NOTA FASE 11 (ajuste de visual):
  - Votos rediseï¿½ados como 3 casillas tipo checkbox VACIAS (28x28, borde 2px):
    se marca la que quieras; al tocarla aparece el simbolo en su color (X roja,
    '-' amarillo, tick verde) con fondo y glow de ese color. El simbolo solo se
    ve en la casilla elegida.
  - Bug CSS corregido: el @media (max-width:640px) del .cyb-dchat estaba SIN
    CERRAR y se tragaba todas las reglas .cyb-trio/.cyb-rem-fab (solo se
    veian en pantallas <=640px). Se cierra el media y se limpia un bloque
    .cyb-dock fantasma duplicado. Barra: smoke completo PASANDO, tsc+lint+
    build OK.

FASE 12 (2026-09-08):
  - Se elimina el ticker "ACCESO PERMITIDO / LA BITACORA NO SE DETIENE"
    (div .cyb-ticker del layout + bloque CSS + animacion cymar).
  - Renombrado: la card de responsabilidades diarias pasa de "Votos" a
    "Objetivos" (header, banner de vista futura, smoke-test, comentario CSS).
  - Sistema de puntos para la Complecion: cada estado vale X=0, "-"=0.5,
    tick=1. lib/completion.ts gana statusValue(), dayScore() y dayPercent();
    isDayFulfilled ahora compara PUNTOS: modo "count" = puntos >= umbral
    (un "a medias" aporta 0.5 asi el dia verde no es tan severo), modo
    "percent" = %(puntos/total) >= umbral.
  - Dia: el header muestra "Objetivos ï¿½ 2/3 ï¿½ 67%". El marcado verde por dia
    del mes acumula puntos (statusValue) por fecha.
  - Stats: el % por dia se calcula con puntos sobre el total de objetivos
    activos; nueva metrica "Eficiencia ï¿½ 30 dias" = sum(dia/puntos objetivos)
    => cada dia aporta 0..1, % = puntos/30. Ej: 20 pts en 30 dias = 66%.
    Grilla del Resumen pasa a 6 celdas.
  - Ajustes: textos actualizados (hecho=1, a medias=0.5, valor en puntos o %).
  - Smoke actualizado (ticker ausente, header con % y baja con "-", renames)
    PASANDO TODO. tsc + lint + next build OK. Sin errores JS.

FASE 13 (2026-09-08):
  - Chat: botones "Cï¿½MO VA EL Dï¿½A" y "Cï¿½MO VA EL MES" (+ input mm/yy
    precargado con el mes actual, editable) sobre el input del chat.
  - Slot de contexto en memoria por request (lib/chat-context.ts):
    buildDayContext() arma un texto plano compacto del dï¿½a actual (hora,
    objetivos con ?/-/? y %, cant. de notas con la ultima truncada,
    aprendizajes y recordatorios, cap ~700 chars); buildMonthContext(mm, yy)
    arma el resumen del mes literal (dias con registro, puntos, eficiencia %,
    mejor dia) + una linea por dia con datos (dd/mm puntos % n a, cap 24
    lineas, ~1200 chars). Ambas regeneran el mismo slot en cada request
    (sobrescribe el anterior) y no entran al historial guardado en el cliente.
  - app/api/chat/route.ts: acepta dataQuery ("day" | {kind:"month",month,year}
    validado) e inyecta el snapshot como mensaje system de contexto entre el
    prompt y el historial, con instruccion de comentar sin repetir ni enumerar.
  - Cliente: send() suma dataQuery; los botones mandan el mensaje visible
    ("ï¿½Cï¿½mo va el dï¿½a?" / "ï¿½Cï¿½mo va el mes (mm/yy)?") + el contexto oculto.
    CSS .cyb-dchat-quick (fila de botones + input mm/yy).
  - Smoke extendido (botones presentes, mm/yy precargado, ambos responden con
    streaming) PASANDO TODO. tsc + lint + build OK. Sin errores JS.

NOTA FASE 13 (2026-09-09):
  - Los botones Cï¿½MO VA EL Dï¿½A / MES (+ input mm/yy) pasaron a la cabecera del
    panel, recien debajo de la fila del nombre del dino (.cyb-dchat-head en
    columna + .cyb-dchat-head-top). Se quito la fila de arriba del input.
  - La hora del snapshot del dia ahora se calcula forzando la zona horaria de
    Argentina ("America/Argentina/Buenos_Aires", UTC-3) con Intl en vez de
    depender de la zona del server: hora HH:MM real y fecha "hoy" real en
    Argentina (aunque el server corra en UTC, ej. Vercel). A las 00:00 de
    Argentina el tono "que temprano" del bot es correcto.

NOTA PRE-DEPLOY (2026-09-09):
  - todayISO() en lib/utils.ts ahora calcula la fecha "hoy" forzando la zona
    de Argentina (America/Argentina/Buenos_Aires) con Intl (helper arNow(),
    tambien devuelve hora HH:MM). Toda la app (dias, stats, guard de dias
    futuros, calendario) queda consistente aunque el server corra en UTC
    (Vercel). chat-context.ts reutiliza arNow() en vez de su helper propio.
  - Arte de dinos redisenado (poses mixtas frontales/perfil) en dinosaurs.ts.
  - Boton Cï¿½MO VA EL MES ahora pide el mes al apretar: aparece un mini-form
    inline mm/yy precargado (autofocus) + OK/?, Enter envia la pregunta y se
    oculta. Flujo "cï¿½mo va el dï¿½a" directo. Verificado con smoke completo.
  - Mobile: @media (max-width:640px) agrega body { zoom: 0.8 } (todo al 80%).
  - Build (webpack)+lint+tsc y smoke al 100% en verde.

NOTA (2026-09-09): BORRADO DE DIA POR TRIPLE TAP
  - Triple tap (3 clicks rapidos, event.detail>=3) sobre una celda del
    calendario (solo dias <= hoy; evitado por long-press de recordatorios)
    abre un modal rojo llamativo (.cyb-modal-danger, borde 2px rojo + glow):
    "Seguro, borrar" -> paso 2 "SI, BORRAR" (doble OK) -> ejecuta
    deleteDayDataAction(date) que borra notas, aprendizajes, recordatorios,
    daily_objectives del dia y la fila days; revalida el dia y el calendario.
    CANCELAR / click afuera cierra sin borrar.
  - lib/actions.ts: deleteDayDataAction() (soporta local y Supabase; los
    deletes encadenados: se borran daily_objectives por day_id ANTES de days).
  - globals.css: .cyb-modal-danger + .cyb-danger-btn / .cyb-danger-btn-fire.
  - Smoke nuevas checks (abre/cancela, no borra data real): TODO en verde.

NOTA (2026-09-09): PWA + NOTIFICACIONES PUSH + NOMBRE/LOGO
  - Nombre de app: manifest name/short_name -> "BitAK0R4_" y metadata
    appleWebApp (iOS muestra "BitAK0R4_" bajo el icono).
  - Logo: scripts/gen-icons.mjs ahora renderiza el ojo ASCII del login
    (geometria de components/login/ascii-eye.tsx, estatico) como bloques
    blancos/grises sobre cuadrado negro. Regenrados icon-192, icon-512 y
    apple-touch-icon.png (180).
  - Notificaciones push (web-push + VAPID):
    * Env nuevas: NEXT_PUBLIC_VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY,
      VAPID_SUBJECT, CRON_SECRET (en .env.local y .env.local.example).
    * Dependencia: web-push (server-only).
    * lib/push-client.ts (tipos PushPayload/StoredSubscription +
      urlBase64ToUint8Array, cliente seguro) y lib/push.ts (server:
      isPushConfigured, sendPush, dailyReviewPayload, reminderTomorrowPayload).
    * app/sw.ts: handlers push (showNotification) y notificationclick
      (navigate + focus / openWindow).
    * lib/actions.ts: savePushSubscriptionAction (upsert on endpoint) y
      deletePushSubscriptionAction.
    * app/api/cron/notify/route.ts: autentica con Bearer CRON_SECRET (o
      ?secret=), manda 1) push diario "revisï¿½ tu bitï¿½cora" y 2) push con los
      recordatorios de MAï¿½ANA (fecha AR). Borra suscripciones 404/410.
    * vercel.json: cron diario "0 1 * * *" (01:00 UTC = 22:00 AR).
    * components/notifications-toggle.tsx: seccion en Ajustes para activar/
      desactivar (permiso + subscripcion). Maneja dev sin SW y permisos.
    * supabase/schema.sql: tabla push_subscriptions (+RLS). En Supabase hay
      que re-correr el schema (idempotente) para que exista la tabla.
  - Verificacion: tsc, lint, build y smoke en verde. En dev el SW no corre
    (serwist disable en development) -> el toggle avisa; el push real se
    prueba en produccion.
  - Tareas pendientes del deploy: correr schema.sql en Supabase; agregar las
    4 env nuevas en Vercel + redeploy; en el celular re-instalar la PWA.

[2026-09-09] MEJORAS DE USO MÃ“VIL + MODELO DE CHAT + PUSH DE PRUEBA
  - Fechas por partes (el "/" era imposible de tipear en celular):
    * Recordatorio (REC+): 3 inputs [dÃ­a] / [mes] / [aa] inputMode=numeric,
      se arma dd/mm/aa al guardar (parseShortDate). Labels:
      "DÃ­a del recordatorio (dd)"/"Mes del recordatorio (mm)"/"AÃ±o del
      recordatorio (aa)".
    * Chat "CÃ³mo va el mes": 2 inputs [mm] / [aa], precargados con el mes
      actual, OK/âœ•/Enter. Labels "Mes (mm)"/"AÃ±o (aa)". CSS .cyb-dchat-month
      con wrap y entradas de 34px.
  - Reloj: se quitÃ³ la animaciÃ³n "blk" de .cyb-clock u -> los segundos
    avanzan sin parpadear (mantienen el verde neÃ³n).
  - Login mÃ³vil: main pasa a min-h-dvh + overflow-x-hidden (scrollea si hace
    falta), gap reducido en pantallas chicas y ojo a min(86vw, 832px).
  - Chat modelo: se quitÃ³ thinkingmachines/inkling-small:free del fallback
    (responde 403: solo en agentic harnesses). Ahora el 403 se trata como
    "probar el siguiente modelo" (junto a 429/404). Default sigue siendo
    google/gemma-4-31b-it:free.
  - BotÃ³n "Probar notificaciÃ³n" en Ajustes (solo cuando hay suscripciÃ³n
    activa): lib/push.ts testPushPayload() + lib/actions.ts
    sendTestPushAction(subscription) que manda un push de prueba al
    dispositivo y muestra el resultado (incluye HTTP status si falla).
  - FIX importante: lib/local-client.ts cacheaba la DB en una singleton de
    mÃ³dulo ("let store"). En el build de producciÃ³n, la Server Action (que
    escribe) y el render de pÃ¡gina (que lee) usan instancias de mÃ³dulo
    DISTINTAS -> la pÃ¡gina seguÃ­a mostrando datos viejos aunque el archivo
    .data/local-db.json sÃ­ se actualizaba (notas/aprendizajes/segmentos
    nunca aparecÃ­an tras guardar). Ahora load() lee el archivo en cada
    operaciÃ³n (archivo chico, solo modo local), garantizando consistencia.
  - smoke-test.js: selectores de mes/recordatorio pasan a los inputs por
    partes; se reemplazÃ³ el mmInput viejo por monthInput/yearInput.
  - VerificaciÃ³n: tsc, lint, build y smoke completos en verde (solo queda el
    429 de rate-limit del modelo gratuito de OpenRouter, tolerado y con
    fallback; no rompe el chat).

[2026-09-09] CHAT DE DINOS DESHABILITADO + RESUMEN MENSUAL CON IA EN STATS
  - Chat de dinosaurios deshabilitado: se comento el import y el render de
    <DinoChat /> en app/bitacora/layout.tsx (el dock .cyb-dock desaparece).
    Todo el codigo (componentes, lib/dino-prompt.ts, chat-context, /api/chat)
    queda intacto para retomarlo mas adelante.
  - Stats: boton "Â¿Como estuvo el mes?" justo debajo de los graficos:
    * app/api/monthly/route.ts (POST, nodejs, maxDuration 60): autenticacion,
      levanta los ultimos 30 dias (mismo rango que los graficos): % diario por
      objetivos, racha, eficiencia, dias con registro/vacios, mejores y peores
      dias, y muestras reales (hasta 15, 300 chars c/u) de notas y
      aprendizajes. Prompt de analista serio/objetivo con estructura fija
      RESUMEN / ALTOS / BAJOS / CIERRE y una recomendacion. Usa los modelos
      :free con fallback por 429/403/404. Si no hay registros devuelve aviso.
    * components/monthly-summary.tsx (use client): boton + estado
      "Analizando el mes..." + bloque .cyb-month-summary con Regenerar; los
      errores (modelos gratis saturados) se muestran con Reintentar.
    * Montado en app/bitacora/stats/page.tsx tras <StatsCharts />.
  - FIX tipos: LocalQuery no tiene .limit() -> las muestras de notas/
    aprendizajes se recortan en JS (slice). daily usa DayStatsPoint (date).
  - smoke-test.js: se quito el bloque del chat (dock/panel/streaming/dia-mes/
    cierre) por "Chat de dinos deshabilitado (sin dock ni panel)" y se
    agregaron asserts del resumen: boton presente, estado analizando y
    .cyb-month-summary con texto (tolerante: acepta resumen o error).
- Verificacion: tsc, lint, build y smoke en verde. El resumen funciono de
    punta a punta; los modelos :free de OpenRouter estaban saturados (429/502)
    y el bloque mostro el aviso con Reintentar (esperado).

[2026-09-09] CHAT DE NUEVO + RECORDATORIOS COMPLETOS + DÃAS DESTACADOS
  - Chat de dinosaurios REHABILITADO: se descomentaron el import y el
    <DinoChat /> en app/bitacora/layout.tsx (vuelve el dock y el panel).
    Modelo sigue gratis a pedido del usuario; se agrega "openrouter/free"
    como ULTIMO respaldo en app/api/chat/route.ts y app/api/monthly/route.ts
    (router gratuito que deriva a cualquier modelo :free disponible -> absorbe
    la saturaciÃ³n/intermitencia del tier sin pagar).
  - Recordatorios "completo":
    * supabase/schema.sql: reminders ganÃ³ completed_at timestamptz (en el
      create + alter table add column if not exists para DB ya creada).
      lib/types.ts: Reminder.completed_at?: string | null.
    * lib/actions.ts: toggleReminderCompleteAction(id, date, completed).
    * components/reminder-panel.tsx: botÃ³n "âœ“ Completar"/"Desmarcar" por Ã­tem
      (optimista, aria-label), los completos se tachan (.cyb-rem-done).
    * Calendario: el recuadro rojo ahora cuenta SOLO recordatorios pendientes
      (app/bitacora/[date]/page.tsx arma pendingRemindersByDate filtrando
      completed_at; calendar-nav recibe pendingReminders).
    * Cron de push: los recordatorios de maÃ±ana excluyen los completados
      (.is("completed_at", null)). lib/local-client.ts: se agregÃ³ el filtro
      .is() (null y undefined cuentan igual).
  - DÃ­a destacado (anillo dorado):
    * supabase/schema.sql: tabla day_flags (date pk, created_at) + RLS +
      Ã­ndice. lib/local-client.ts: "day_flags" en TABLES.
    * lib/actions.ts: toggleDayFlagAction(date) (upsert/delete + revalidate).
    * components/day-flag-button.tsx (nuevo): botÃ³n "â˜… Destacar"/"Destacado âœ“"
      en la cabecera del bloque Agenda de la vista del dÃ­a.
    * calendar-nav.tsx: prop flaggedDates -> .cyb-num.has-flag (anillo dorado
      amber, convive con hoy/recordatorio/futuro) + tooltip "DÃ­a destacado".
    * globals.css: .cyb-num.has-flag (+ combos con today/has-reminder/future),
      .cyb-flag-btn (.on dorado) y .cyb-rem-done (tachado).
  - Banner de dÃ­as futuros: se quitÃ³ "PodÃ©s mirarlo en gris y" -> queda
    "PodÃ©s dejar recordatorios" (se mantiene el aviso de solo lectura).
  - smoke-test.js: chat reactivado (dock -> panel -> 6 dinos -> cierra),
    banner sin "en gris", marcar/desmarcar completo (rojo desaparece/vuelve),
    destacar/quitar (anillo dorado aparece, persiste al reload y desaparece).
  - Verificacion: tsc, lint, build y smoke en verde. El Ãºnico error JS es el
    502 esperado del resumen mensual (modelos gratis saturados -> aviso con
    Reintentar). Server de producciÃ³n reiniciado en http://localhost:3000.
  - Deploy pendiente: re-correr supabase/schema.sql en Supabase (crea
    day_flags y agrega completed_at a reminders) + redeploy. Sin env nuevas.

[2026-09-09] PROVEEDOR DE RESPALDO: GROQ (fallback cuando OpenRouter se satura)
  - El usuario eligiÃ³ no pagar; mantiene OpenRouter como principal y suma Groq
    gratis como respaldo. No se pierde nada: se conservan los modelos :free +
    openrouter/free.
  - lib/llm.ts (nuevo, server-only): chatWithFallback() unifica la lÃ³gica que
    estaba duplicada en /api/chat y /api/monthly:
    * Orden de intentos: 1) OpenRouter (env + :free + router gratuito),
      2) Groq (openai/gpt-oss-120b) si hay GROQ_API_KEY. Si ambos se agotan
      (429/403/404/5xx) -> mensaje "Los modelos gratis estÃ¡n saturados".
    * Soporta streaming SSE (chat, stream:true -> pipe de deltas) y respuesta
      completa (resumen mensual, stream:false -> parsea choices[0].message.
      content; respuesta vacÃ­a -> intenta el siguiente proveedor).
    * Sin ninguna clave -> 503 con aviso.
  - app/api/chat/route.ts: queda fino (auth, validaciÃ³n, dataQuery, prompt) y
    delega en chatWithFallback{stream:true, temp 0.5, max 800}.
  - app/api/monthly/route.ts: se eliminaron DEFAULT_MODELS/MODELS/fetch/
    summarize; el POST llama a chatWithFallback{stream:false, temp 0.3,
    max 700} y devuelve {text} o {error} (502/503).
  - env: .env.local suma GROQ_API_KEY (real; .env.* estÃ¡ gitignored, nunca se
    commitea). .env.local.example: placeholder GROQ_API_KEY=. Para Vercel:
    agregar GROQ_API_KEY como secret.
  - Ojo modelo Groq 2026: "llama-3.3-70b-versatile" ya no existe en Groq
    (model_not_found). Se probaron los disponibles y funcionales:
    openai/gpt-oss-120b (200) y qwen/qwen3.6-27b (429 por OTPM). Se usa
    openai/gpt-oss-120b (verificado: 200 + streaming SSE con deltas).
  - Verificacion: tsc, lint, build y smoke PASANDO TODO sin errores JS. El
    resumen mensual funcionÃ³ de punta a punta con texto real (vÃ­a Groq cuando
    OpenRouter se saturÃ³): en el smoke entrÃ³ y devolviÃ³ "RESUMEN ... 2 dÃ­as
    (06%)...". Server de producciÃ³n reiniciado en http://localhost:3000.

[2026-09-09] VOCES DE LOS DINO: MÃS PERSONALIDAD + RIO PLATENSE FUERTE
  - FIX BUG: la personalidad del anquilosaurio nunca cargaba. El registro usa
    id "ank" pero el archivo era lib/dinos/4nky.md -> readPersonality(id)
    devolvÃ­a "" y entraba con reglas genÃ©ricas nomÃ¡s. git mv 4nky.md -> ank.md.
  - lib/dino-prompt.ts (GLOBAL_RULES reforzado):
    * Voseo explÃ­cito: imperativos -Ã¡/-Ã©/-Ã­; prohibido espaÃ±ol neutro (tÃº,
      usted, vosotros, haz, hazlo, recuerda, puedes, "ten en cuenta",
      "sin embargo", "por lo tanto", "no dudes en").
    * LÃ©xico rioplatense natural sin amontonar: che, dale, bancÃ¡, re-, Â¿viste?,
      ta, ni en pedo, de onda, pila de, tranqui/cortito. Prohibido deformar
      palabras.
    * Cierre del prompt (resumen): "usÃ¡ el registro de los ejemplos de TU VOZ".
  - 6 .md reescritos con formato comÃºn: TU VOZ (rasgos lÃ©xicos + frase fetiche)
    + REGLA DE ORO + 3 EJEMPLOS few-shot (dueÃ±o -> dino) en rioplatense.
    Distintivos: TYrA_ seco/imperativo + "boludo" esporÃ¡dico; N0d-0 cÃ¡lido,
    sin boludo; 4nky monolito de 1-2 frases, sin jerga fuerte; V3L0 chispa +
    "Â¿Esperar? Ni en pedo, boludo"; BR4ch0 pausa/panorama, sin jerga fuerte;
    Tr1l0_ orden/listas de 3 pasos, sin jerga fuerte. Se conservan rol y
    especie.
  - Verificacion: tsc, lint, build, smoke TODO VERDE sin errores JS.
  - Auditoria por voz (script temporal node -> /api/chat con cookie calculada):
    los 6 suenan distinto y en rioplatense, tirando contexto real del dÃ­a
    (ej. ank: "No hay que correr. AguantÃ¡: a las veinte y cuarenta y tres
    tenÃ©s una meta cumplida..."). Server reiniciado.

[2026-09-09] FRASES MOTIVACIONALES: POOL GRANDE (REALES + JUEGOS + ANIME) + GLITCH MÃS LENTO
  - lib/quotes.ts reescrito: 123 frases en total.
    * Reales (~73): se quitaron 8 sobrantes repetidas/planas (dup de Jobs,
      Churchill, Einstein y Confucio, + Wooden, Rohn, Ziglar, Ward) y se sumÃ³
      variedad de gente/paÃ­ses: CortÃ¡zar (ARG), Facundo Cabral (ARG), Galeano
      (URY), Coelho (BRA), Neruda (CHL), GarcÃ­a MÃ¡rquez (COL), Kahlo (MEX),
      BolÃ­var (VEN), Marco Aurelio (ROM), TolstÃ³i (RUS), Rumi (PERS), Gibran
      (LBN), Mandela (ZAF), Sun Tzu (CHN), Teresa de Calcuta, Dante, Ana Frank,
      Buda, Picasso, Wilde, Tagore. Se quedan los 2 proverbios chinos, 2 anÃ³nimas
      sÃºper conocidas (se sacÃ³ SOLO Â«Todo lo que siempre quisiste estÃ¡ al otro
      lado del miedoÂ» y los 13 refranes populares.
    * Juegos (25): Paarthurnax, Geralt, Kratos, Viejo Sabio (Zelda), Andrew Ryan,
      G-Man, Ezio, Deckard Cain, Vaas, Narrador Fallout, Arthur Morgan, Pod 042
      (NieR), "AguantÃ¡ y sobrevivÃ­", Cave Johnson, Zhongli, hoguera de Dark Souls
      ("no te atrevas a volverte hueco"), Sora, Undertale ("a pesar de todo seguÃ­s
      siendo vos"), Elizabeth, Solid Snake, Joel, Leon, Kane, Nathan Drake, Master
      Chief.
    * Anime (25): Geto, Gojo, Yuji, Itachi, Luffy, Zoro, Saitama, Naruto, Kakashi,
      Rock Lee, Armin, Rengoku, Thorfinn, Nanami, Spike, Hanamichi, Kamina, Goku,
      Gon, Tanjiro, All Might, Eren, Edward Elric, Kenny, Plus Ultra.
  - Cada una con rol (autor, contexto de obra) como las reales; sin tag de
    categorÃ­a en la UI. Textos adaptados a rioplatense cuando lo permite la frase.
  - VerificaciÃ³n por web de las citas de ficciÃ³n antes de escribirlas (sustituidas
    las no verificables, ej. el quote de 2B que era nihilista -> Pod 042 "el futuro
    no se te regala"; Goku usa la del cap. 228 del manga "hasta el de mÃ¡s humilde
    origen puede superar a la elite"; Kamina usa la del ep. 8 "creÃ© en el vos que
    cree en vos mismo"; Eren la del tema de la libertad "nacemos libres... al diablo
    con ellos"; Nanami la de la temp 1 ep 9 sobre las pequeÃ±as desilusiones).
  - Glitch de la cita 2x mÃ¡s lento para que se lea: cygA 2.3s -> 4.6s y cygB 1.9s -> 5.7s
    (app/globals.css, .glitch::before/::after).
  - VerificaciÃ³n: count script (73 reales + 25 juegos + 25 anime = 123), tsc, lint y
    build en verde. Server de producciÃ³n reiniciado en http://localhost:3000 (smoke
    completo no corrido: el cambio es solo de datos frases + animaciÃ³n CSS).

[2026-09-09] CHAT DE DINOS DESHABILITADO (de nuevo)
  - app/bitacora/layout.tsx: se quitÃ³ el import y el render de <DinoChat /> (el dock
    .cyb-dock desaparece). Todo el cÃ³digo (componentes, lib/dino-prompt.ts,
    chat-context, /api/chat) queda intacto para retomarlo mÃ¡s adelante.
  - scripts/smoke-test.js: el bloque del chat (dock -> panel -> 6 dinos -> cierre)
    se reemplazÃ³ por la aserciÃ³n "Chat de dinos deshabilitado (sin dock)" +
    "Sin botÃ³n de abrir chat".
  - VerificaciÃ³n: tsc, lint, build en verde + probe rÃ¡pido (playwright): vista dÃ­a
    carga, sin .cyb-dock, sin botÃ³n abrir chat y sin panel; sin errores JS. Server
    de producciÃ³n reiniciado en http://localhost:3000.

[2026-09-11] PUSH ELIMINADO + IGNORAR OBJETIVO
  - Se borró todo el stack de notificaciones web push (no funcionaba y se decidió
    no seguir lidiando con suscripciones/VAPID; para notificar en el celu con la app
    cerrada no existe alternativa sin servidor, y los Notification Triggers
    showTrigger/TimestampTrigger fueron abandonados por Chrome).
  - Archivos eliminados: components/notifications-toggle.tsx, lib/push.ts,
    lib/push-client.ts, app/api/cron/notify/route.ts (+ carpeta) y vercel.json
    (solo tenía el cron).
  - lib/actions.ts: fuera la sección "Notificaciones push"
    (savePushSubscriptionAction, deletePushSubscriptionAction, sendTestPushAction)
    y sus imports.
  - app/sw.ts: se quitaron los handlers push/notificationclick (se mantiene el
    Serwist para el offline caching). .env.local.example: bloque VAPID/CRON fuera.
  - supabase/schema.sql: tabla push_subscriptions (+ su RLS) fuera.
  - package.json: se desinstaló web-push y @types/web-push (npm uninstall).
  - PENDIENTE EN SUPABASE (hosted): drop table if exists push_subscriptions;
  - PENDIENTE EN VERCEL: borrar NEXT_PUBLIC_VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY,
    VAPID_SUBJECT y CRON_SECRET (quedan sin uso).
  - NUEVO: "ignorar objetivo" por día. 4to estado en daily_objectives
    (status='ignored') que excluye el objetivo del numerador Y del denominador del
    % del día, del marcado verde del calendario, de stats/eficiencia y del contexto
    de chat. Si un día queda con TODOS los objetivos ignorados es día NEUTRO (no
    castiga la eficiencia).
  - lib/types.ts: ChecklistStatus suma "ignored". lib/completion.ts: statusOf lo
    acepta, statusValue=0, dayPercent y el nuevo countedItems() descartan los
    ignorados. Supabase: constraint de daily_objectives.status pasó a
    ('none','partial','done','ignored').
  - components/objectives-checklist.tsx: botón "ignorar" a la derecha de cada
    objetivo, confirmación por SEGUNDO TOQUE ("¿Seguro? sí / no"); cuando está
    ignorado la fila queda atenuada con "ignorado · quitar".
  - app/bitacora/[date]/page.tsx: header "Objetivos · X/Y · Z%" y marcado verde del
    calendario usan solo objetivos en cuenta (activos - ignorados del día).
  - app/bitacora/stats/page.tsx, /api/monthly y lib/chat-context.ts: por día,
    denominador = totales - ignorados (días todo-ignorados neutros).
  - scripts/smoke-test.js: bloque "2b) Notificaciones push" -> aserciones de que la
    sección NO existe ("Sin sección Notificaciones_push", "Sin Activar
    notificaciones"); agregadas checks de ignorar (arma '¿Seguro?', cancelar
    desarma, resta 1 al denominador, persiste tras reload, quitar restaura) y se
    arregló la aserción del anillo dorado para apuntar a la celda de HOY (antes
    buscaba ausencia global de .has-flag y quedaba colgada por un flag viejo de
    09-09 en la DB local).
  - Verificación: tsc, lint y build en verde; smoke completo en verde (incluido el
    resumen mensual de IA) sin errores JS. Server de producción reiniciado en
    http://localhost:3000.

[2026-09-11] EFEMÉRIDES: FECHAS QUE SE REPITEN TODOS LOS AÑOS
  - Nueva pestaña "Efemérides" en el nav (entre Hoy y Ajustes, ruta
    /bitacora/efemerides). Mismo patrón visual y de comportamiento que un
    recordatorio común pero SIN opción de completar (solo Editar / Borrar) y
    anclado a día+mes (se repite todos los años: cumpleaños, aniversarios).
  - Tabla nueva annual_reminders (id, month 1-12, day 1-31, content,
    created_at) + índice + RLS en supabase/schema.sql; tipo AnnualReminder en
    lib/types.ts; "annual_reminders" en lib/local-client.ts (TABLES + seed []).
  - lib/utils.ts: isValidMonthDay (Feb admite hasta 29, como referencia bisiesta),
    formatMonthDay (dd/mm) y annualOccurrenceISO(month, day) -> próxima fecha
    (este año o el que viene; 29/02 en no-bisiesto cae 28/02) con "en N días".
  - lib/actions.ts: create/update/deleteAnnualReminderAction (validan la fecha,
    revalidatePath("/bitacora", "layout")).
  - components/annual-reminders.tsx (client): filas dd/mm · contenido con hint de
    próxima ("hoy"/"mañana"/"en N días"), Editar inline / Borrar (sin completar),
    formulario con inputs día/mes (sin año), useOptimistic igual que reminders.
  - Vista del día: bloque propio ".blk.efemeride-blk" (acento ámbar) justo ENCIMA
    de la cita (MotivationalQuote) listando las efemérides de esa fecha.
  - Calendario: las celdas con efeméride del mes se marcan con borde punteado
    (.cyb-num.has-annual, convive con hoy/flag/recordatorio/futuro) + tooltip
    "Efeméride"; se pasan como prop annualDates desde la página del día.
  - smoke-test.js: checks nuevos (link en nav, página carga, agregar/editar/
    borrar, "próxima: hoy", celda .has-annual, bloque encima de la cita y que
    desaparece al borrar). TODO en verde + sin errores JS. tsc, lint, build OK.
  - Server de producción reiniciado en http://localhost:3000 (PID 7692).
  - PENDIENTE EN SUPABASE (hosted): correr el create table de annual_reminders
    (idempotente, ya está en supabase/schema.sql).

[2026-09-11] LA HOJA: PENSAMIENTOS EXTENSOS DEL DÍA
  - Botón con forma de hoja de papel en la cabecera de "Agenda" (junto a ★
    Destacar) que abre un cuaderno blanco con renglones y margen rojo para volcar
    pensamientos/hojas de notas extensas del día. Autosave con debounce de 1,5 s
    y estado "Editando…/Guardando…/Guardado · hora/Error". Tope BLANDO de ~20.000
    caracteres (solo aviso de tinta, no bloquea).
  - Dato de almacenamiento: la DB local anda en ~4,3 KB de datos y el plan free de
    Supabase recién entra en modo read-only a los 500 MB de DB (proyecto nuevo
    arranca ~40-60 MB de sistema). Por eso la hoja usa 1 fila por día máximo:
    tabla journal (date PK, content, updated_at) + RLS en supabase/schema.sql;
    la fila se crea al primer guardado con texto y se BORRA si queda vacía.
  - lib/types.ts: JournalEntry + DayMark.thought; lib/local-client.ts: tabla
    "journal" + seed []; lib/actions.ts: saveJournalAction (upsert por date,
    si queda vacío hace delete, revalidatePath("/bitacora/{fecha}")).
  - components/journal-sheet.tsx (client): botón .leaf-btn con doblez de esquina,
    modal .paper-shell con .cyb-paper (renglones alineados al line-height 28px +
    línea roja del margen), flush al cerrar/desmontar por si quedó algo sin
    guardar, formato de hora es-AR en el status.
  - Vista del día: fetch del journal del día (maybeSingle) + fechas del mes con
    journal para el 4to segmento; el botón se monta con key={fecha} para no
    arrastrar el estado entre días.
  - Calendario: 4to segmento blanco .segs-w bajo el número cuando el día tiene
    hoja escrita (convive con verde/amarillo/rojo). CSS .leaf-btn/.leaf-lines,
    .paper-shell, .cyb-paper/.cyb-paper-in, .paper-status/.paper-warn, .segs-w.
  - smoke-test.js: bloque "5b) La Hoja" (botón en cabecera, apertura del
    cuaderno, autosave "Guardado · hora", tope blando muestra el aviso, 4to
    segmento aparece/desaparece, persiste tras reload, vaciar borra la fila y el
    segmento). Se subieron hoyIso/iso() arriba del archivo para reutilizarlos.
  - Verificación: tsc, lint y build en verde; smoke completo en verde (95 checks)
    sin errores JS. Server de producción reiniciado en http://localhost:3000.
  - PENDIENTE EN SUPABASE (hosted): correr el create table de journal
    (idempotente, ya está en supabase/schema.sql) junto con annual_reminders.

[2026-09-11] CATEGORÍAS PARA EFEMÉRIDES
  - Se puede armar categorías en la pestaña Efemérides (ej: "cumpleaños",
    "aniversarios") para ordenar cada fecha que se anota. Crear, renombrar
    (✎ inline), reordenar con flechas ▲▼ y borrar (doble toque de
    confirmación: primero arma, segundo confirma).
  - Tabla nueva annual_categories (id, name, sort_order, created_at) + RLS en
    supabase/schema.sql; annual_reminders suma category_id (FK on delete set
    null). NULL = "sin separar". Tipos AnnualCategory + AnnualReminder.
    category_id en lib/types.ts; "annual_categories" en lib/local-client.ts
    (TABLES + seed []). LocalQuery suma .ilike() y .limit() para soportar el
    check de nombre duplicado y el último sort_order.
  - lib/actions.ts: createAnnualCategoryAction (rechaza duplicados case-,
    insensitive), updateAnnualCategoryAction, moveAnnualCategoryAction (swap
    de sort_order con el vecino), deleteAnnualCategoryAction (primero pasa las
    efemérides de esa categoría a category_id NULL = "sin separar", después
    borra la categoría). create/updateAnnualReminderAction ahora reciben
    categoryId.
  - components/annual-reminders.tsx: bloque "Categorías" en chips ámbar
    (.cat-chip con ✎/▲/▼/✕), lista de efemérides AGRUPADA por categoría con
    "Sin separar" al final, y un select compacto por fila para MOVER cada
    efeméride de categoría. Al dar "Agregar efeméride": si hay categorías
    aparece el menú "¿Dónde la guardo?" (elegís la categoría o "Sin separar";
    si cerrás sin elegir, se anota igual como "sin separar"). Sin categorías
    guarda directo como antes.
  - app/bitacora/efemerides/page.tsx: fetch de annual_categories (order
    sort_order) y pasa initialCategories. La vista del día NO cambia (por
    elección: las categorías se manejan solo en la pestaña).
  - Estilos nuevos en globals.css: .chip-row, .cat-chip (con botones y estado
    .cat-del-active de confirmación), .chip-btn (botones del menú), .cat-move-select.
  - smoke-test.js: bloque "11a2)" (crear categoría, segunda categoría,
    renombrar, menú al guardar con elección de categoría, cerrar sin elegir ->
    "sin separar", mover con el select, agrupación bajo la categoría, flechas
    reordenan, borrar con doble toque deja las efemérides en "sin separar" y
    cleanup). Se sacaron los casts TS de los evaluate (el archivo es JS puro).
  - Verificación: tsc, lint y build en verde; smoke completo en verde (106
    checks) sin errores JS. Server de producción reiniciado (PID 19992).
  - PENDIENTE EN SUPABASE (hosted):
      create table if not exists annual_categories (
        id         uuid primary key default gen_random_uuid(),
        name       text not null,
        sort_order int not null default 0,
        created_at timestamptz not null default now()
      );
      alter table annual_reminders add column if not exists category_id uuid
        references annual_categories(id) on delete set null;
      alter table annual_categories enable row level security;

[2026-09-11] DOCUMENTACIÓN, SCRIPTS DE MANTENIMIENTO Y SMOKE AMPLIADO
  - README.md reescrito con la documentación real y completa del proyecto:
    presentación, caraterísticas por secciones (login/ñoño, bitácora, metas
    temporales, efemérides con categorías, recordatorios, la hoja, PWA),
    modos de datos LOCAL (sin Supabase) y SUPABASE, requisitos, puesta en
    marcha, scripts, cómo correr el smoke, resumen mensual con IA y claves,
    deploy en Vercel y notas de la base de datos.
  - .gitignore: se versiona .env.local.example (`!.env.local.example`) para
    que los scripts de puesta en marcha tengan la plantilla.
  - package.json: scripts nuevos `check` (`tsc --noEmit && eslint`),
    `smoke` (`node scripts/smoke-test.js`) y `backup`
    (`node scripts/backup-db.js`).
  - scripts/backup-db.js: copia .data/local-db.json a
    .data/backups/local-db-<marca de tiempo>.json conservando las últimas 10
    copias.
  - smoke-test.js: bloques nuevos al inicio de "Ajustes" ->
    "2c) Metas temporales" (crear con rango=HOY, mensaje, vista del día,
    desactivar/reactivar, editar, borrar y sin rastro) y
    "2d) Modos de completado" (self-contained: count + umbral, percent +
    umbral, suma de − = 0.5, restaura count + mínimo). El bloque 2c destapó
    un bug REAL en modo LOCAL: al crear una meta temporal no se seteaba
    is_active (la vista del día la filtra) -> arreglado en lib/actions.ts
    (insert con is_active: true).
  - smoke-test.js: bloque "11a)" robustecido (el menú "¿Dónde la guardo?"
    bloquea el guardado si hay categorías: ahora elige "Sin separar") y
    "11a2)" con pre-cleanup de categorías sobrantes de corridas fallidas y
    verificación que el borrado persista pasada la ventana optimista
    (espera antes de dar por confirmada la ausencia).
  - Cometidos del locale durante la corrida: la escritura read-modify-write
    del local-client pierde escrituras si varias acciones salen en paralelo;
    el smoke serializa las acciones destructivas (espera entre clicks).
  - Verificación: `npm run check` (tsc + eslint) en verde, `npm run build`
    en verde, smoke completo en verde (todos los checks booleanos imprimen
    true/yes) sin errores JS. Server de producción reiniciado con el build.
  - PENDIENTE EN SUPABASE (hosted): correr supabase/schema.sql (bloques
    idempotentes) y limpiar en Vercel las variables de entorno de push
    (NEXT_PUBLIC_VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_SUBJECT,
    CRON_SECRET) y los enpoints/scripts de cron de resumen mensual si no se
    usan.

[2026-09-11] ROLLBACK — SE ELIMINA EL FEATURE DE TEMAS VISUALES
  - Después de probar 3 diseños del tema "renacimiento" (v1, v2 barroco y
    v3 "El Gabinete"), el usuario decidió que ninguno lo convencía y pidió
    volver atrás en TODO lo relacionado a temas.
  - Se revirtió el working tree completo al commit 2e00bbe (estado previo al
    feature) con `git restore .` + borrado del archivo nuevo
    components/theme-settings-form.tsx.
  - Queda eliminado: el formulario "Tema visual" en Ajustes, el tipo
    ThemeName + columna settings.theme (schema.sql, seed y migración del
    local-client), saveThemeAction en lib/actions.ts, las fuentes
    Montserrat/Playfair y la cookie theme en app/layout.tsx y app/page.tsx,
    el branch renacimiento del login (login-screen.tsx), el refactor de
    password-gate a .login-*, las clases .cyb-ok/.cyb-err/.theme-* y los
    bloques html[data-theme="renacimiento"] de globals.css, el barrido de
    hex a utilidades, y el bloque "11g) Tema visual" del smoke (vuelve a
    arrancar sin cookie).
  - Verificación post-rollback: `npm run check` y `npm run build` en verde
    (las rutas /_not-found y /manifest.webmanifest vuelven a ser estáticas),
    smoke completo en verde (sin :false ni FALLO, sin errores JS), login con
    el HUD cyberpunk original. Server de producción reiniciado.
  - Nota: la fila settings del local-db.json puede quedar con un campo
    `theme` extra que el código (ya revertido) ignora; es inofensivo.

[2026-09-11] LOGIN V2 — "TERMINAL DE ACCESO" (rediseño manteniendo esencia)
  - Decisión del usuario: mantener el núcleo (ojo ASCII sentinela, lluvia
    digital, frases existenciales, apagado CRT al fallar) y mejorarlo SIN
    seguimiento de cursor (uso mayoritario en mobile). Se optó por una
    "terminal de acceso": el ojo te lee por foco y sus miradas tienen más
    vida; sin cursor-mouse.
  - ascii-eye.tsx: prop stare -> mode "idle"|"read"|"stare". Idle con
    coreografía nueva (miradas largas, micro-sacadas, barridos que "escanean
    la sala"). read = mira de frente con micro-temblor y parpadea más seguido
    (al tocar/Tab en el input, onReading lo dispara). stare = mirada fija +
    pupilA dilatada (umbral pupilar PUPIL_K - bias) antes del flash CRT.
    Misma matemática de esfera; el resto del shader intacto.
  - Nuevos componentes en components/login/: osd-frame.tsx (barra superior
    "ACCESO RESTRINGIDO" + dot LIVE parpadeante, barra inferior
    "BITÁKORA OS v0.1" + reloj HH:MM:SS, scanlines CSS y viñeta), boot-log.tsx
    (tipea 3 líneas de arranque, salteable con tap/tecla, reduced-motion
    instantáneo), typewriter.tsx (frase tipeada con caret verde y
    data-phrase-src para el smoke).
  - password-gate.tsx: onReading(true/false), contador cosmético "· INTENTO N ·",
    flash rojo -> verde + shake de la fila al errar, focus-visible ring. Sin
    lockout (reintento infinito como antes).
  - login-screen.tsx: phases boot -> idle -> wrong -> flash -> off. El input
    NUNCA se condiciona al boot (DOM presente desde el inicio, el smoke no se
    rompe). El ojo "prende" con fade (opacity transition) tras el boot.
  - login-phrases.ts: +4 frases ("El ojo ya te vio llegar", "No hay puertas.
    Solo mirada.", etc.).
  - globals.css: .login-scanlines, .login-vignette, dot-live-blink,
    tw-caret-blink, gate-shake-x.
  - Smoke (login): + OSD "ACCESO RESTRINGIDO", reloj #HH:MM:SS, scanlines,
    ojo (.login-eye), typewriter completo (span.textContent === data-phrase-src),
    contador "INTENTO 1" tras clave errónea. Resto del flujo intacto (negro
    absoluto, refresco, bitakra -> /bitacora).
  - Verificación: npm run check y npm run build en verde. Smoke completo en
    verde (ningún ": false", sin FALLO, sin errores JS). Server reiniciado.
  - Nota eslint strict (react-hooks v16): nada de setState síncrono en bodies
    de effect ni mutar refs en render -> boot/typewriter/osd usan setState
    sól desde callbacks de interval, y los refs de callback solo se actualizan
    en effectos.
